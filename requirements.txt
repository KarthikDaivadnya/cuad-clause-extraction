pdfplumber==0.11.4
pandas==2.2.3
numpy==1.26.4
python-dotenv==1.0.1
requests==2.32.3
tqdm==4.66.5

# LLM providers (only the one(s) you use actually need a valid API key)
groq==0.11.0
openai==1.54.4
anthropic==0.39.0
# Pinned: httpx 0.28+ removed the `proxies` kwarg that groq/openai/anthropic's
# SDKs (as pinned above) still pass internally, causing
# "TypeError: Client.__init__() got an unexpected keyword argument 'proxies'"
httpx==0.27.2

# Bonus: semantic search
sentence-transformers==3.2.1
faiss-cpu==1.9.0
scikit-learn==1.5.2

# Needed by the demo notebook (docs/../demo_pipeline_walkthrough.ipynb) to
# render real CUAD contract text into PDFs when data/demo_contracts/ is empty
reportlab==4.2.5

# Optional interactive demo
streamlit==1.39.0

# Tests
pytest==8.3.3